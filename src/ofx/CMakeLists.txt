# OFX Plugin CMakeLists.txt
# This will be implemented in later tasks

message(STATUS "OFX directory created - implementation pending")

# Placeholder for OFX plugin build
# add_library(cinema_pro_hdr_ofx MODULE ${OFX_SOURCES})
# target_link_libraries(cinema_pro_hdr_ofx cinema_pro_hdr_core)